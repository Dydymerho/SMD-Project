apiVersion: 1

providers:
  - name: "SMD Dashboards"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards # Đường dẫn trong container (sẽ mount ở bước 4)
